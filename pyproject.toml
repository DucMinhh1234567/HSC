[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hsc-edu"
version = "0.1.0"
description = "Hierarchical Semantic Chunking for Educational Textbooks"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["hsc_edu*"]
